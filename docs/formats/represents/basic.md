# Basic

The basic format of most data is represented via [TOML v1.1.0](https://toml.io/en/v1.1.0).

You can miss some fields out, which will use its default value (program-defined default value, not always variable-type default value).
