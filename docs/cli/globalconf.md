# GlobalConf

The cli should read from a global (user specific) config file for certain values.

## Default

The default file is `~/.declmysys`, and is in _toml_ format.

## Values

- `dotdecldir`, the directory path of _dotdecldir_

## Example

```toml
dotdecldir = "~/Dotdecl/"
```
